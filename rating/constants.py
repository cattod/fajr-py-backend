import configs
import os

dir_path = os.path.dirname(os.path.abspath(__file__))
RATING_ADD_SCHEMA_PATH = '{}/{}'.format(dir_path,'schemas/add.json')
RATING_EDIT_SCHEMA_PATH = '{}/{}'.format(dir_path,'schemas/edit.json')
