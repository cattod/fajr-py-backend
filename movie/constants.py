import configs
import os

dir_path = os.path.dirname(os.path.abspath(__file__))
MOVIE_ADD_SCHEMA_PATH = '{}/{}'.format(dir_path,'schemas/add.json')
MOVIE_EDIT_SCHEMA_PATH = '{}/{}'.format(dir_path,'schemas/edit.json')
